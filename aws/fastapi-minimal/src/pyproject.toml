[project]
name = "fastapi-minimal"
version = "0.1.0"
description = "Minimal FastAPI + MongoDB Lambda app for Atlas Landing Zone demos"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.116.1",
    "mangum>=0.19.0",
    "pydantic-settings>=2.7.1",
    "pymongo[aws]>=4.14.1",
    "uvicorn>=0.35.0",
]
