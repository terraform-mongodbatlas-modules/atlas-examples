atlas_org_id = "<YOUR_ATLAS_ORG_ID>"
cluster_name = "hybrid-search-ui"

# Pin compute to cap size (disk GB still auto-scales). Default is auto-scaling M10–M200.
# manual_scaling = { instance_size = "M10" }

# Cheap personal lab (before first apply):
# cluster_type = "REPLICASET"

# Skip AWS interface VPC endpoints (ECR, logs, Secrets Manager, STS). NAT stays
# on via ecs_apps.ui.internet_egress; AWS API calls go NAT to public endpoints.
# Does not skip Atlas PrivateLink or the S3 gateway. About $2.40/day off in
# us-east-1 with two AZs.
# vpc_config = { skip_interface_endpoints = true }

# Skip WAF (not the WebSocket or upload fallback; lab should keep WAF on):
# http_edges = { main = { waf = { enabled = false } } }

# Opt-in SCRAM + one IPv4 for laptop mongosh / local app (not on the happy path):
# public_debug_access = { ip_address = "203.0.113.10" }

# Optional LLM (just create-llm-secret, then paste the name).
# LLM_PROVIDER is inferred from llm_env_name.
#
# Anthropic (default):
# llm_secret_name = "hybrid-search-ui-llm"
# llm_env_name    = "ANTHROPIC_API_KEY"
# llm_env = {
#   ANTHROPIC_MODEL = "claude-sonnet-4-20250514"
# }
#
# OpenAI:
# llm_secret_name = "hybrid-search-ui-llm"
# llm_env_name    = "OPENAI_API_KEY"
# llm_env = {
#   OPENAI_MODEL    = "gpt-4o"
#   OPENAI_BASE_URL = "https://your-endpoint/v1"
# }
#
# Gemini:
# llm_secret_name = "hybrid-search-ui-llm"
# llm_env_name    = "GEMINI_API_KEY"
# llm_env = {
#   GEMINI_MODEL = "gemini-2.5-flash"
# }
#
# Grove:
# llm_secret_name = "hybrid-search-ui-llm"
# llm_env_name    = "GROVE_API_KEY"
# llm_env = {
#   GROVE_BASE_URL = "https://grove.example.mongodb.com/v1"
#   GROVE_MODEL    = "gpt-4o"
# }

# Lab (skip module-managed CMK, log export, backup export):
# atlas_integrations = {
#   encryption      = { enabled = false }
#   log_integration = { enabled = false }
#   backup_export   = { enabled = false }
# }
